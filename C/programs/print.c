// Import to use printf 
#include <stdio.h>

int main() {
    // TODO Print all data types
    char welcome[] = "hello!";
    printf("Greeting message: %s", welcome);
    // Return success code
    return 0;
}